# Copy to .env (which is gitignored) and replace with your own OpenRouter key.
OPENROUTER_API_KEY=your-openrouter-api-key
# Alternatively, use the alias expected by some hosts:
# OPEN_ROUTER_KEY=your-openrouter-api-key
